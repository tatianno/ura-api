server {
    listen 80;
    server_name backend;

    location /static/ {
        root /usr/src/ura-api/src/static/;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/usr/src/ura-api/src/app.sock;
    }
}